import java.util.ArrayList;  //which over class we use in java , they use package


import tools.*; //import all files from tools package // * means all the files.

public class Demo {
    public static void main(String[] args) {
        // Calc obj=new Calc();
        AdvCalc obj1=new AdvCalc();
        ArrayList list=new ArrayList<>();

    }
}
